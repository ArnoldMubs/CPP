#ifndef PLATEAU_HPP_
#define PLATEAU_HPP_

#include <iostream>

enum Jetton : int {ROUGE,JAUNE,LIBRE};

class Plateau {
  private : 
    Jetton[7][6] _cellules;
    int[7] _placeLibre;
  
  public :
    Plateau();
  void updatePlaceLibre(int col);
    int getPlaceLibre(int col) const;
  bool ajouterJetton(Jetton jetton);
  void afficher();
  
}
#endif
